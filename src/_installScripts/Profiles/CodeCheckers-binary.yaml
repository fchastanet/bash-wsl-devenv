extends:
  - "${BASH_DEV_ENV_ROOT_DIR}/src/_commandDefinitions/installScript.yaml"

vars:
  SRC_FILE_PATH: src/_installScripts/Profiles/CodeCheckers-binary.yaml

compilerConfig:
  targetFile: "${BASH_DEV_ENV_ROOT_DIR}/installScripts/CodeCheckers"
  relativeRootDirBasedOnTargetDir: ..
binData:
  commands:
    default:
      functionName: CodeCheckersCommand
      version: "3.0"
      copyrightBeginYear: 2024
      definitionFiles:
        11: ${BASH_DEV_ENV_ROOT_DIR}/src/_installScripts/Profiles/CodeCheckers.sh
      commandName: CodeCheckers
